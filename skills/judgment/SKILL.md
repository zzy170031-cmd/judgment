---
name: "judgment"
description: Lightweight alias for the Judgment workflow pack. Use when the user says judgment, Judgment skill, Judgment Controller, or asks to evolve or run the local Judgment project workflow; route through 666, work-planner, work-splitter, 555, and the repo rules instead of duplicating their bodies.
---

# judgment

This is a short alias for the Judgment skills pack.

Use it when the user says `judgment`, asks to evolve Judgment itself, or expects the local Judgment workflow to coordinate project routing, Agent Office visibility, Controller state, evidence, Git/worktree boundaries, or 555 review.

## Route

1. Treat `judgment` as the pack name, not a separate executor.
2. Load and use `666` for routing unless the user already named a narrower skill.
3. Use `work-planner` for fuzzy planning, `work-splitter` for decomposition, and `555` only for milestone/release/backend/shared-surface/AI-safety evidence closure.
4. Keep the authority boundary intact: Markdown guides, HTML displays and requests, Codex executes, scripts/tests verify, and user/QA/555 accepts.

## Forbidden

- Do not duplicate the full `666` body here.
- Do not let Agent Office HTML execute shell, Git, install, deploy, delete, or external-send actions.
- Do not mark Judgment evolution complete without a failing-capable validation command and Git state check.
