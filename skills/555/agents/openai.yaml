interface:
  display_name: "555 Five-Agent Loop"
  short_description: "Runs XA/XB milestones, release gates, AI/Agent safety reviews, backend delegation, adversarial review, evidence closure, and context guard."
  default_prompt: "Use $555 to run this XA/XB milestone, release-readiness claim, or AI/Agent safety review through the five-agent evidence loop with context-pressure protection."

policy:
  allow_implicit_invocation: false
