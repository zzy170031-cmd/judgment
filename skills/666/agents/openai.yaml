interface:
  display_name: "666 Workflow Router"
  short_description: "Routes Hope work to the smallest useful skill, role, gate, or thread."
  default_prompt: "Use $666 to choose the smallest safe workflow route for this Hope task before editing or dispatching."

policy:
  allow_implicit_invocation: true
