interface:
  display_name: "666 Workflow Router"
  short_description: "Routes Codex project work by skill, role, gate, thread, and context pressure."
  default_prompt: "Use $666 to choose the smallest safe workflow route and context handoff point for this Codex project task."

policy:
  allow_implicit_invocation: true
